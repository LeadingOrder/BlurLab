/**
 * The first concrete blur transform.
 *
 * Implement horizontal neighbour averaging directly before introducing a
 * generic kernel or convolution abstraction. This file should depend only on
 * PixelBuffer and boundary-coordinate helpers.
 */
export {};
