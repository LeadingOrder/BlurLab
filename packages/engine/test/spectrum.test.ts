import { describe, it } from "vitest";

describe("analyzeSpectrum", () => {
    it.todo("returns aligned, finite frequency and decibel arrays");
    it.todo("places constant-image energy at the zero-frequency bin");
    it.todo("gives the identity kernel a zero-decibel response");
    it.todo("matches input and output spectra for the identity kernel");
    it.todo("uses the complete periodic image in global mode");
    it.todo("uses a centered Hann-windowed sample in local mode");
    it.todo("preserves magnitude under a circular spatial shift");
});
